# Секреты — репозиторий публичный, ключ утёкший однажды не отзывается
.env
.env.local
*.key

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / IDE
.DS_Store
.idea/
.vscode/

# Runtime
*.log
.cache/

# База агентов: в ней лежат тексты диалогов, а репозиторий публичный.
# WAL-режим кладёт рядом с базой ещё два файла — их ловим отдельно,
# иначе половина истории уехала бы в репозиторий в *.db-wal.
*.db
*.db-wal
*.db-shm
/data/
